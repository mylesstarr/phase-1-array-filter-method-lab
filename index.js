const drivers = [ {"name":"driver 1", "hometown": "Chicago" 
},

{"name":"driver 2", "hometown": "New York" 

}
]

function findMatching(drivers, name){
    const filteredDrivers = drivers.filter(name)
    if (filteredDrivers === "name") 
    return filteredDrivers
}